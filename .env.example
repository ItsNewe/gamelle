# Traefik API base URL. If unset, autodiscover probes common locations.
TRAEFIK_API_URL=http://traefik.example.com:8080

# Optional: extra comma-separated candidate base URLs for autodiscover.
TRAEFIK_DISCOVER_CANDIDATES=
